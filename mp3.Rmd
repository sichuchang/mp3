---
title: "mp3"
author: "Chujun He"
date: "11/11/2017"
output: html_document
---

```{r}
library(mdsr)
library(RMySQL)
db <- dbConnect_scidb(dbname = "imdb")
newtable<-db %>%
  dbGetQuery("SELECT t.id, t.title, t.production_year,
             mi.info as info
             FROM title t
             Join movie_info as mi on mi.movie_id = t.id
             where info_type_id = 3
             having production_year IS NOT NULL;")
```

```{r}
#a smaller table, from year 1901-1999
filmsmall = newtable%>%
  filter(production_year>1900 & production_year<2000)
filmsmall
```
```{r}
new = filmsmall%>%
  group_by(id,production_year)%>%
  summarise(sum = n_distinct(info))%>%
  arrange(production_year)
new
```

```{r}
#find the mse of each year
```

```{r}
#plot the mse of each year
```
